import { DndCopySourceDirective } from './dnd-copy-source.directive';

describe('DndCopySourceDirective', () => {
  it('should create an instance', () => {
    const directive = new DndCopySourceDirective();
    expect(directive).toBeTruthy();
  });
});
