import { DndCopyDestinationDirective } from './dnd-copy-destination.directive';

describe('DndCopyDestinationDirective', () => {
  it('should create an instance', () => {
    const directive = new DndCopyDestinationDirective();
    expect(directive).toBeTruthy();
  });
});
